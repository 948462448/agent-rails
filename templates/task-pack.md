# Agent Task Pack

> Generated by Agent Rails.

## Session Marker

AGENT RAILS: ON (mode=<pack-mode>, pack=<task-pack-path>)

- User-visible opening line: `AGENT RAILS: ON (mode=<pack-mode>, pack=<task-pack-path>)`.
- If this pack is intentionally skipped later, say `AGENT RAILS: SKIPPED (reason=<reason>)` instead of staying silent.

## Goal

Describe the concrete user goal here.

## Context Budget

- Model:
- Pack mode:
- Grill question budget:
- Chars/token estimate:
- Total:
- Git state:
- Changed file sort:
- Memory cards:
- Verification suggestions:
- Contract/checklist:

## Current Git State

- Project:
- Branch:
- Target ref:
- HEAD:
- Base ref:
- Merge base:

## Changed Files

- None detected yet.

## Changed File Priority

- None detected yet.

## Changed File Excerpts

- No changed text file excerpts selected.

## Task Code Evidence

- No task-relevant tracked code evidence matched the goal.

## Working Tree Status

```text
Clean.
```

## Relevant Entry Docs

- `AGENTS.md`

## Context Gaps

- None detected.

## Agent Rails Contract

### Trigger Matrix

- Deep pack: 2+ subprojects, API/contracts/schema/data-model changes, ADR/handbook work, migrations/refactors, or ambiguous product decisions.
- Lite pack: POCs, quick prototypes, version/Dockerfile/OSS/deploy prep, codegen freshness checks, or continuation from an existing handbook.
- Check only: deploy/release/upload workflows that consume the current branch, plus final delivery after code/doc/script changes.
- Skip pack: pure status queries, simple command output, or fixed operations with no repo change and no branch-consumption risk.

### Role In This Task

- Agent Rails should actively shape the work, not only summarize it.
- Use changed-file priority and excerpts to pick the first files to inspect, then verify by reading the real source.
- Use memory cards only when they match the task or changed paths.
- For refactors, identify behavior invariants, dead-code hypotheses, and verification loops before editing.

### Workflow Rules

- Search -> read -> verify -> deliver.

### Grill Gate

- Before architecture, refactor, migration, API contract, data model, or ambiguous product work, run a short grill first.
- Ask one decision question at a time and provide your recommended answer.
- If the answer can be found in the repo, inspect the repo instead of asking the user.
- Default to at most 8 grill questions, then move non-blocking choices into deferred decisions.
- In lite mode, skip full grill and ask only blockers.
- Skip grill for small mechanical edits with clear requirements.

### Memory Sync Rules

- Memory is the cross-session long-term truth; Task Pack is the current-session slice.
- If Task Pack evidence disagrees with memory, treat memory as stale until verified.
- Task Pack generation must not write memory; final delivery should run memory curator or record a skip reason.

### Quality Gates

- Code changes need runnable verification or a clear reason verification was not run.
- Deploy/release skills should start with Agent Rails check print-only as Step 0 when they consume the current branch.

### Failure Rules

- After repeated failures, change strategy and summarize proven facts.

## Subagent Result Contract

- Goal handled
- Scope and files inspected
- Findings or output
- Changes made
- Evidence collected
- Commands run
- Verification not run and why
- Memory candidates, if any
- Open questions or blockers
- Recommended next action

## Project Configuration

- Domain map:
- Domain docs:
- ADR directory:
- Agent docs:
- Issue tracker:
- Triage labels:

## Memory Provider

- Mode:

## Memory Cards

- No local cards selected.

## Verification Suggestions

```text
Run agent-rails check.
```

## Delivery Checklist

- What changed
- What was verified
- What was not verified
- Residual risks
- Next action suggestions: fix / do not fix / later
